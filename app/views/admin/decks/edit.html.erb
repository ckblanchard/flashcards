<h1>Edit <%= @deck.organization.name %> deck</h1>

<%= render 'form' %>

<%= link_to 'Show', @deck %> |
<%= link_to 'Back', admin_decks_path %> | 
<%= link_to 'Delete', @deck, method: :delete, data: { confirm: 'Are you sure?' } %>
