<h1>Organizations using %Flashcards%</h1>

<table>
  <tr>
    <th>Organization</th>
  </tr>

<% @organizations.each do |organization| %>
  <tr>
    <td><%= organization.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add your Organization', new_organization_path if user_signed_in? %>
